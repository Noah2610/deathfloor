#!/bin/bash

# shellcheck source=./share.sh disable=SC2155
_dir="$( dirname "$0" )"
source "${_dir}/share.sh"
unset _dir

CODEGEN_UNITS="256"

RUSTFLAGS="-C codegen-units=${CODEGEN_UNITS}" \
    "${ROOT}/bin/run" "$@"
