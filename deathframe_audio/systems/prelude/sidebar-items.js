initSidebarItems({"struct":[["PlaySoundsSystem","Plays queued sounds from `SoundPlayer` components. If a resource of component type `SoundPlayer` exists, then it will also be used to play sounds. `SoundAction::Play` sounds are played with the default volume, which can be set with the `with_default_volume` builder function. See the `Default` implementation for the default."],["UpdateSongPlaybackSystem",""],["UpdateSongStateSystem","For now, this system only sets a `Song`'s `PlaybackState` to `Stopped` if it's `AudioSink` is empty. If the song is supposed to loop, then it is restarted if it's `AudioSink` is empty."],["UpdateSongVolumeSystem","Handles updating the default audio output sink's volume, used with `Songs`."]]});